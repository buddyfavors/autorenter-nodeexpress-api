'use strict';

const express = require('express');
//const passport = require('passport');
//const config = require('../config');

const router = express.Router();

// TODO: Fill in with auth routes

module.exports = router;
